group {<xray-group-statement>} end

<xray-group-statement> :==

  type=hl
  --{Type of group.}
  object=<xray-object>
  --{Object to be added to group. The order of object statements
     determines the order of the objects within the group.}

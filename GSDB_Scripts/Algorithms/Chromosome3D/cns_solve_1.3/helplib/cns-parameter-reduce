reduce <parameter-reduce-statement> end

<parameter-reduce-statement> :==

  selection=<atom-selection>
  --{Default=(all)}
  overwrite=<logical>
  --{Default=true}
  mode=average|statistics
  --{Default=statistics}
  
  --{Only active energy terms, defined by the flags statement, are reduced.}

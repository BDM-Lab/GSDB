ensemble <noe-ensemble-statement> end

<noe-ensemble-statement> :==

  on|off
  --{}
  dimer <segid> <segid>
  --{Allowed interactions for dimers.}
  occupancy=<logical>
  --{Use occupancies as weighting factors for distance averaging.}
  reset
  --{Reset dimer interaction list.}

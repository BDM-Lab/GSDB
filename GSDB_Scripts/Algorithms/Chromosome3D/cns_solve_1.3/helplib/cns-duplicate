duplicate <duplicate-statement> end

<duplicate-statement> :==

  selection=<atom-selection>
  --{Select atoms for duplication.}
  segid=<segid>|residue=<resid>
  --{New segid name or residue number for duplicated atoms. Either segid
     or residue has to be specified in order to distinguish the duplicated
     atoms from the current atoms.}
